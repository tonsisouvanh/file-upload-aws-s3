# Upload image to aws s3 (React + TypeScript + Vite)
1. Singup for aws
2. Create bucket
3. Config permission: Bucket policy & Cross-origin resource sharing (CORS)
4. Create IAM user to get api key in order to access the bucket